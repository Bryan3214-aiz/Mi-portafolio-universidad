#Explicacion de metodos con Dalto y su estructura es: Dato.metodo()
#cualquier funcion que no tenga un objeto no puede ser una funcion.

#La funcion upper() hace que la cadena de texto selecionada o escrita en la misma variable se vuelva mayuscula.
#La funcion lower() hace que toda la cadena de texto se vuelva miniscula.
#La funcion capitalize() hace que la primera letra de toda linea de texto se vuelva mayuscula.

#la funcion busqueda_find() o busqueda_index() buscan la posicion de cualquier valor o cada de texto en una variable.

#la funcion isnumeric() si es un valor numerico devuelve true.
#la funcion isalpha() devuelve true si es alfanumerico.

#la funcion count() devuelve cuantas veces se repite el valor o la cadena de texto solicitada por ejemplo 2,2,2 se encontrarian 3 coincidencias.
#la funcion lent() cuenta cuantos caracteres tiene la cadena de texto. como se usa: len(valor1).

#la funcion replace() sustituye cadena1 por lo que contenga cadena2. ejem: cadena1 = "hola" resultado = cadena1.replace("hola","hello")

#la funcion split() separa todo los valores que contenga la cadena por el parametro dado. este metodo es el unico que devuelve una matriz

valor1 = "hola soy bryan"

resultado = valor1.index
print (resultado)